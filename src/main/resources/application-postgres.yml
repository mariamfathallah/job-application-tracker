server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/jobtracker
    username: jobtracker
    password: jobtracker
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    open-in-view: false
  liquibase:
    change-log: classpath:db/changelog/db.changelog-master.xml
    enabled: true
app:
  jwt:
    secret: "dev-secret-change-me-dev-secret-change-me-dev-secret-change-me"
    expiration-minutes: 3600000